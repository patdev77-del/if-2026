<script setup lang="ts">
// ---------------------------------------------------------------------------
// Day 2 exercise — build this component from scratch
// ---------------------------------------------------------------------------

// TODO Day 2: import the Repo type from '@/types'
// import type { Repo } from '@/types'

// TODO Day 2: define typed props using defineProps<{}>()
// The component should accept a single `repo` prop of type Repo
//
// defineProps<{
//   repo: Repo
// }>()
</script>

<template>
  <!-- TODO Day 2: build a card that displays:
       - repo.name as a heading (link to repo.html_url)
       - repo.description
       - repo.language
       - repo.stargazers_count
       - repo.topics as tags (hint: v-for)
  -->
  <div class="project-card">
    <p class="placeholder">ProjectCard — build me on Day 2!</p>
  </div>
</template>
